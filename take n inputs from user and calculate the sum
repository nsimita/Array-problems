#include <stdio.h>
 
int main() {
    int num[100];
    int i,n, sum=0;
    printf("How many numbers: ");
    scanf("%d",&n);
    for(i=0; i<=n-1;i++){
        scanf("%d",&num[i]);
    }
    
    for (i=0; i<=n-1; i++){
        sum= sum+num[i];
    }
    printf("%d\n",sum );
    return 0;
}
